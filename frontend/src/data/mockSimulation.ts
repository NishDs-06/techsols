// GUTTED: All mock simulation logic removed.
// These are empty stubs to preserve import compatibility.
// The dashboard now relies entirely on live WebSocket data from the orchestrator.

export const startSimulation = () => { };
export const stopSimulation = () => { };
